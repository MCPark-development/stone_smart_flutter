name: stone_smart_mcpark

packages:
  - stone_smart_mcpark
  - stone_smart_mcpark/example

scripts:
  analyze:
    run: melos exec -- flutter analyze .
  format:
    run: melos exec -- dart format .

command:
  version:
    linkToCommits: true
    workspaceChangelog: true

sdk: ">=3.0.0 <4.0.0"

repository: https://github.com/MCPark-development/stone_smart_flutter
